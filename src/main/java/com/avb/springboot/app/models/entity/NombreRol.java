package com.avb.springboot.app.models.entity;

//Clase enum "NombreRol" para definir los nombres de los roles existentes en la tabla "roles" de la base de datos

public enum NombreRol {
    ROLE_USER,
    ROLE_PM,
    ROLE_ADMIN
}
